#JSGF V1.0;

grammar module;
public <command> = ( bomb check | <success> | <failure> | defuse <module> | add a strike | reset wire sequences );
<static> = ( e | a | eye | oh | ooh | awe );
<module> = ( wires | button | keypads | simon says | words | memory | morse code | complicated wires | sequence | maze | password | knobs );
<success> = (the bomb is defused | we did it);
<failure> = the bomb (detonated | blew up);